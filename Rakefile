# Add your own tracks in files placed in lib/tracks ending in .rake,
# for example lib/tracks/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_tasks
